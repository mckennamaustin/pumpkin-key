import React from 'react';
import styled, { css } from 'styled-components';

const BackButton: React.SFC<{}> = props => <Button {...props}>Back</Button>;

const Button = styled.button``;

export default BackButton;
